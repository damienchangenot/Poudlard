{% extends 'layout.html.twig' %}

{% block title %}{{ parent() }} Reinitialiser votre mot de passe{% endblock %}

{% block body %}
    <section class="container pyt-5">
    <h1 class="text-center"> Reinitialiser votre mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <div class="text-center">
            <button class="btn btn-secondary rounded-pill">Réinitialiser</button>
        </div>
    {{ form_end(resetForm) }}
    </section>
{% endblock %}
