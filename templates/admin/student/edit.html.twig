{% extends 'layoutADmin.html.twig' %}

{% block title %}{{ parent() }} Modifier un élève{% endblock %}

{% block body %}
    <section class="container mt-5 pb-5">
    <h1 class="text-center">Modifier un élèves</h1>

    {{ include('admin/student/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <div class="text-center my-3">
     {{ include('admin/student/_delete_form.html.twig') }}
        </div>
        <a class="btn btn-secondary rounded-pill" href="{{ path('admin_student_index') }}">Retour à l'index</a>
    </section>
{% endblock %}
