{% extends 'layoutAdmin.html.twig' %}
{% block title %}{{parent()}}Edit Teacher{% endblock %}

{% block body %}
    <h1>Edit Teacher</h1>

    {{ include('admin_teacher/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_teacher_index') }}">back to list</a>

    {{ include('admin_teacher/_delete_form.html.twig') }}
{% endblock %}
